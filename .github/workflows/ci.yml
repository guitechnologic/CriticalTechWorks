name: CI Pipeline

on:
  push:
    branches: ["main"]
  pull_request:

# Permissões necessárias para publicar imagem no GHCR
permissions:
  contents: read
  packages: write

jobs:
  lint-and-test:
    name: Lint + Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run linter (Ruff)
        run: ruff check .

      - name: Run tests
        run: pytest

  docker:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    needs: lint-and-test   # só roda se testes passarem

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login no GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker image
        run: |
          docker build -t ghcr.io/${{ github.repository }}/ctw-app:latest .

      - name: Push Docker image
        run: |
          docker push ghcr.io/${{ github.repository }}/ctw-app:latest