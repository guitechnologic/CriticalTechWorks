[defaults]
inventory = inventory/hosts.ini
host_key_checking = False
remote_user = ec2-user
private_key_file = ~/.ssh/id_rsa

[ssh_connection]
ssh_args = -o ProxyCommand="ssh -W %h:%p -i ~/.ssh/id_rsa ec2-user@BASTION_PUBLIC_IP"